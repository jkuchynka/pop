doctype html
html(lang='en', ng-app='app')
  head
    meta(charset='utf-8')
    meta(http-equiv='X-UA-Compatible', content='IE=edge')
    meta(name='viewport', content='width=device-width, initial-scale=1.0, user-scalable=no')

    title Pop

    base(href='/')

    link(rel='shortcut icon', href='favicon.ico')

    // inject:vendor:css
    // endinject

    // inject:css
    // endinject

  body(ng-controller="RootCtrl")

    div(growl)

    div.container-fluid(ng-class="navClass()")
      nav.navbar.navbar-default.navbar-inverse.navbar-fixed-top(role='navigation', ng-class="navClass()")
        .container-fluid
          .navbar-header
            ul.nav.navbar-nav
              li
                a(href="", ng-click="showLeftNav = !showLeftNav")
                  i.fa.fa-2x.fa-bars
              li
                a.navbar-brand(href='#/')
                  i.fa.fa-lg.fa-bolt
                  | &nbsp;Pop

          div(user-menu)

      div.view(ng-view)

    div.subnav.subnav-left(nav-menu)
    div.subnav.subnav-right(nav-menu-right)

    // inject:vendor:js
    // endinject

    // inject:js
    // endinject

    script. angular.module('app').constant('CSRF_TOKEN', 'INJECT_CSRF_TOKEN');

    if includeBrowserSync
      script(src='//10.0.0.11:3000/socket.io/socket.io.js')
      script. var ___socket___ = io.connect('http://10.0.0.11:3000');
      script(src='//10.0.0.11:3001/client/browser-sync-client.0.6.2.js')

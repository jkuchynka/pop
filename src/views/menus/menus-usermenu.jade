
div.nav.navbar-nav.navbar-right(ng-if="user.id")
    p.navbar-text Yo, &nbsp;
        a.profile-link(ui-sref="users.profile", ng-bind="user.username")
        | &nbsp; !

    a.navbar-toggle-slideout(href="#", ng-click="toggleNav('right')")
        i.fa.fa-bars

ul.nav.navbar-nav.navbar-right(ng-if="!user.id")
    li
        a(ui-sref="login") Login

    li
        a(ui-sref="register") Register
